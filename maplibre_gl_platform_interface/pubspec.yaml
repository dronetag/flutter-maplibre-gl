name: maplibre_gl_platform_interface
description: A common platform interface for the maplibre_gl plugin. This package is only intended to be used by the maplibre_gl package.
version: 0.26.0+1
publish_to: https://dart.cloudsmith.io/dronetag/software/
repository: https://github.com/dronetag/flutter-maplibre-gl
issue_tracker: https://github.com/dronetag/flutter-maplibre-gl/issues
resolution: workspace

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0
